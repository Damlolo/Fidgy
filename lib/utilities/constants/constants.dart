abstract class Constants {
  const Constants._();

  static const appName = 'Fidgy';
  static const toastAnimationDuration = Duration(milliseconds: 400);
  static const toastDefaultDuration = Duration(seconds: 4);
}
